<Page
    x:Class="TestRelativePanel.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
    mc:Ignorable="d">

	<StackPanel>

		<RelativePanel Width="400" Height="100" Background="Aquamarine" >
			<Button Content="Button" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True"
					RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignBottomWithPanel="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
		</RelativePanel>

		<RelativePanel Width="400" Height="100" Background="Aquamarine" Margin="0,20,0,0" >
			<ComboBox RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True"
					  RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignBottomWithPanel="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
		</RelativePanel>

		<RelativePanel Width="400" Height="100" Background="Aquamarine" Margin="0,20,0,0" >
			<TextBox Text="TextBox" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True"
					 RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignBottomWithPanel="True" />
		</RelativePanel>

	</StackPanel>

</Page>
